{
    "name": "@race-schedule/admin",
    "version": "0.1.0",
    "private": true,
    "description": "Admin console - Cloudflare Worker (protected by Cloudflare Access) for internal operations such as feature flag management",
    "main": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts",
        "./src/*": "./src/*.ts"
    },
    "scripts": {
        "test": "bun test",
        "test:watch": "bun test --watch",
        "type-check": "tsc --noEmit",
        "build": "tsc --noEmit",
        "dev": "wrangler dev",
        "deploy:test": "bunx wrangler deploy --env test",
        "deploy:production": "bunx wrangler deploy --env production",
        "lint": "eslint './src/**/*.ts'",
        "lint:fix": "eslint --fix './src/**/*.ts'"
    },
    "dependencies": {
        "@race-schedule/core": "workspace:*",
        "hono": "^4.12.32",
        "reflect-metadata": "^0.2.2",
        "tsyringe": "^4.10.0",
        "zod": "^4.4.3"
    }
}
